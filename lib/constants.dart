import 'package:flutter/material.dart';

const pagePadding = EdgeInsets.symmetric(horizontal: 8, vertical: 10);
const kmainFontColor = Color.fromARGB(255, 15, 15, 15);
const kPrimaryColor = Colors.lightBlue;
